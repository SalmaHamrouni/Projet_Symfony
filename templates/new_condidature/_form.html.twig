{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group has-success has-feedback">{{ form_widget(form.Contenu_c,{'attr': {'class': 'form-input', 'for': 'inputSuccess', 'placeholder': '...'}}
        )}}
    </div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
